export type response = {
  value: string | boolean | number;
};

export type formResponseArr = {
  formResponse: response[];
};

export type responseMap = {
  name: string;
  value: string | boolean | number;
};
