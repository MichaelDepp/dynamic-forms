import { Box } from "@chakra-ui/react";
import FormViewer from "components/FormViewer";

const Form = () => {
  return (
    <Box mb={8} w="full">
      <FormViewer />
    </Box>
  );
};

export default Form;
