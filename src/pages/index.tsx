import { Box } from "@chakra-ui/react";
import FormBuilder from "components/FormBuilder";

const Home = () => {
  return (
    <Box mb={8} w="full">
      <FormBuilder />
    </Box>
  );
};

export default Home;
